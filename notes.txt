The project contains a backend in python app.py to communicate with a postgreSQL database to trace trees.
It allow to login as superuser, as city or as user. It contains some table with some characteristics of the trees.
THen in frontend folder there is app.js and index.html to make the user capable to insert a new  tree, review the history and see the trees in the map thanks to the coordinates.
Now the requirements changed. I have to reproduce a database with the tree entries in Schede_Rilevamento_Arete/Schede_Rilevamento_ARETE_DEMO_ver.2.0.xlsm in the sheet 4 (ORD)
you already parsed the xlsm, and you can find dropdowns_ord.py with all the possible choices for the entries, and the ord_calculator.py, which is the same goal of the web app we are developing.
The entries in the postgreSQL must be the current ones plus the one allowing the ord_calculator being implemented in the web app. Some other infos are already dropped in lookup_tables.py
for each entry the html must show the possibilites you dorpped down in dropdowns_ord.
When a new tree is added, all the entries (a part for RILEVATORE which is the user, and ID which is the custom ID) must appear with the multiple choice in a tent menu.
moreover i want to maintain the autodetection of the coordinates via gps.
Try to cosume the smallest number of token you can. 